COM RECURSIVIDADE:

void imprimirArvore(Arvore* f, int *soma){
  if(f != NULL){
    imprimirArvore(f->esq,soma);
    printf("%d\n", f->valor);
    *soma += f->valor;
    imprimirArvore(f->dir,soma);
  }
}

SEM RECURSIVIDADE:

