#include <stdio.h>

int main(){

	float a = 0,i = 1, n;
	
	printf("Digite o numero: ");
	scanf("%f", &n);
	while(i < n+1){
		a = a + (n-i + 1)/i;
		
		i++;
	}
	
	printf("A: %.1f\n", a);
	
	return 0;
}

