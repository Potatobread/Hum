#include <stdio.h>
#include <string.h>

int main(){
    
    int i = 0, media = 0,salario, tempo, total;
    
    //Salario do operario;
    //Tempo de serviço em MESES;
    
    //Total de operarios de serviço inferior a 24 meses;
    //Tempo médio de serviço;
    //Porcentagem de operarios que recebem salario > 600;
    
    while(i < 10){
    	printf("%d - Digite o Numero: ", i+1);
    	scanf("%d", &n);
    	
    	
			
		i++;
	}
	
	printf("\nTotal de Positivos: %d\n", positivos);
	printf("Total de Negativos: %d\n", negativos);
	printf("Total de Nulos: %d\n", nulos);
	media = media/i;
	printf("Media dos Negativos: %.1f\n", media);
	printf("Soma dos Numeros: %d\n", soma);
    
    return 0;
}
